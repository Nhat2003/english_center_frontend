# EnglishCenterFrontend

TOEIC DOWNTOWN Management System - Frontend Application

This project was generated with [Angular CLI](https://github.com/angular/angular-cli) version 9.0.6.

## 🚀 Quick Start

```bash
# Install dependencies
npm install

# Start development server
npm start

# Navigate to
http://localhost:4200/
```

## 🔑 Test Accounts

Default credentials for testing:

| Role | Username | Password |
|------|----------|----------|
| Admin | `admin` | `123456` |
| Student | `student` | `123456` |
| Teacher | `teacher1` | `123456` |


### Features

- ✅ Realtime bidirectional communication
- ✅ Message persistence (database)
- ✅ Auto-reconnect on network failure
- ✅ JWT authentication
- ✅ Load chat history
- ✅ Only chat with classmates (authorization)


### Development server

Run `ng serve` for a dev server. Navigate to `http://localhost:4200/`. The app will automatically reload if you change any of the source files.

Alternative:
```bash
npm start  # Same as ng serve with more memory
```

### Code scaffolding

Run `ng generate component component-name` to generate a new component. You can also use `ng generate directive|pipe|service|class|guard|interface|enum|module`.

### Build

```bash
# Development build
ng build

# Production build
ng build --prod
```

The build artifacts will be stored in the `dist/` directory.

### Running unit tests

Run `ng test` to execute the unit tests via [Karma](https://karma-runner.github.io).

### Running end-to-end tests

Run `ng e2e` to execute the end-to-end tests via [Protractor](http://www.protractortest.org/).

## 📚 Further Documentation

- [WebSocket Testing Guide](WEBSOCKET_TESTING.md)
- [Quick Start Guide](WEBSOCKET_QUICKSTART.md)
- [Implementation Summary](IMPLEMENTATION_SUMMARY.md)

